### Project to test maven versioning and Jenkins pipeline
It contains:
* basic tests
* jenkins pipeline to build app for Pull Requests
* jenkins pipeline to build app, increment app version and deploy(xD) for manual Build